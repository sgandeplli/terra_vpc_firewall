pipeline {
    agent any
    
    environment {
        GOOGLE_APPLICATION_CREDENTIALS = credentials('gcp-sa') 
    }

    stages {
        stage('Hello') {
            steps {
                git branch: 'master', url: 'https://github.com/sgandeplli/terra_vpc_firewall.git' 
            }
        }
        stage('Hello2') {
            steps {
                sh "terraform init" 
                sh "terraform plan"
                sh "terraform apply -auto-approve"
            }
        }
    }
}
